import { ExaminationDictionary } from './examination-dictionary';

describe('ExaminationDictionary', () => {
  it('should create an instance', () => {
    expect(new ExaminationDictionary()).toBeTruthy();
  });
});
