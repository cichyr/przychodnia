import { ShortPhysicalExamination } from './short-physical-examination';

describe('ShortPhysicalExamination', () => {
  it('should create an instance', () => {
    expect(new ShortPhysicalExamination()).toBeTruthy();
  });
});
