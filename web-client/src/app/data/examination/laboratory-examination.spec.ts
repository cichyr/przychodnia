import { LaboratoryExamination } from './laboratory-examination';

describe('LaboratoryExamination', () => {
  it('should create an instance', () => {
    expect(new LaboratoryExamination()).toBeTruthy();
  });
});
