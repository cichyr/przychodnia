import { ReceptionistVisitList } from './receptionist-visit-list';

describe('ReceptionistVisitList', () => {
  it('should create an instance', () => {
    expect(new ReceptionistVisitList()).toBeTruthy();
  });
});
